# Publish the portfolio repository

Suggested repository name: `reviewlens`

Suggested description: Product-review intelligence with verifiable analytics, browser-based semantic retrieval, and an AI/ML TPM delivery case study.

Suggested topics: `rag`, `semantic-search`, `typescript`, `transformers-js`, `product-analytics`, `technical-program-management`.

1. Create an empty repository in your GitHub account named `reviewlens`. Choose Public if you want recruiters to read it. Do not initialize a README or license; this package already has documentation. Third-party notices explain the licensing boundary.
2. Extract the supplied repository ZIP and open a terminal inside `reviewlens`.
3. Run the commands below, using your own repository URL if different.

```bash
git init -b main
git add .
git commit -m "Introduce ReviewLens portfolio project"
git remote add origin https://github.com/Saahil-Gupta1997/reviewlens.git
git push -u origin main
```

GitHub may ask you to sign in. Never paste credentials into source files.

After pushing, check the Actions result, add the description and topics, and pin the repository on your profile. The hosted workspace is private; do not present it as a public recruiter demo. Use the included local demo or record the three-minute walkthrough.

Lead interviews with the product problem, the cost constraint, evidence-versus-generation trade-off, and the QA defect-to-regression story. Be explicit about Codex assistance and pending real-model validation. Do not claim measured business impact until it has been measured.
