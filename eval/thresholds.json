{
  "note": "Two gate sets. 'regression' is enforced on every CI run and is pinned to the measured lexical baseline: its job is to catch a retrieval regression, not to certify quality. 'semantic_release' is enforced only when a run supplies captured on-device hits (--hits): it is the bar the on-device semantic feature must clear before it can ship as a default mode. Raising a bound to make a failing build pass is a process violation and requires a dated entry in docs/delivery/decision-log.md.",
  "baseline_recorded_on": "2026-09-12",
  "baseline_measured": {
    "retriever": "lexical-baseline",
    "split": "dev",
    "recall_at_5": 0.2,
    "precision_at_5": 0.18,
    "mrr": 0.4,
    "absent_topic_false_positive_rate": 0.0,
    "comment": "Mean recall across five relevant-topic development questions over 24 fictional reviews. Not a population accuracy estimate."
  },
  "gates": {
    "regression": {
      "applies_when": "always",
      "recall_at_5": {
        "min": 0.19
      },
      "precision_at_5": {
        "min": 0.17
      },
      "absent_topic_false_positive_rate": {
        "max": 0.3
      },
      "p95_latency_ms": {
        "max": 250
      },
      "rationale": "Pinned at the measured lexical baseline with a small tolerance for floating-point summation. Passing this gate means retrieval has not got worse. It does NOT mean retrieval is good."
    },
    "semantic_release": {
      "applies_when": "a run supplies captured on-device hits via --hits",
      "recall_at_5": {
        "min": 0.7
      },
      "precision_at_5": {
        "min": 0.5
      },
      "absent_topic_false_positive_rate": {
        "max": 0.25
      },
      "p95_latency_ms": {
        "max": 1500
      },
      "rationale": "Portfolio acceptance targets recorded before captured semantic measurements, after implementation. They apply to this development sample, not production trust. Latency requires paired browser worker timings and excludes initial indexing/download; missing evidence cannot pass. Quality bounds are unchanged."
    }
  }
}
